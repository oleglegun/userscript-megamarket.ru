{
  "name": "megamarket.ru",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build": "npm run compile && npm run bundle && npm run add_tm_header",
    "bundle": "npx esbuild dist/index.js --minify --bundle --outfile=pages/userscript.js",
    "compile": "npx tsc --outDir dist",
    "add_tm_header": "cat scripthead | cat - pages/userscript.js > temp.txt && mv temp.txt pages/userscript.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "esbuild": "0.19.6"
  }
}
